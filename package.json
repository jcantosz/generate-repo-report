{
  "name": "csv-combiner-root",
  "version": "1.0.0",
  "description": "Root package for CSV combiner actions",
  "private": true,
  "workspaces": [
    "common",
    "combine-columns",
    "rows-to-columns",
    "post-process"
  ],
  "scripts": {
    "build": "npm run build:combine-columns && npm run build:rows-to-columns && npm run build:post-process",
    "build:combine-columns": "cd combine-columns && npm run build",
    "build:rows-to-columns": "cd rows-to-columns && npm run build",
    "build:post-process": "cd post-process && npm run build"
  },
  "author": "",
  "license": "ISC"
}