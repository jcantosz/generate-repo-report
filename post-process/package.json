{
  "name": "csv-post-process",
  "version": "1.0.0",
  "description": "Post-process CSV data using configurable rules",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd ../common && npm install",
    "build": "ncc build index.js -o dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "ajv": "^8.17.1"
  },
  "type": "module"
}
